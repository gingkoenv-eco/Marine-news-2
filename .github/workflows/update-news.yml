name: Update marine news

on:
  schedule:
    # Runs once a day at 06:00 UTC. Change the number before "*" to pick a different hour.
    - cron: "0 6 * * *"
  workflow_dispatch: {}
    # ^ lets you trigger this manually from the "Actions" tab on GitHub, useful for testing

permissions:
  contents: write

jobs:
  update-news:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Fetch news and build news.json
        run: python fetch_news.py

      - name: Commit and push news.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add news.json
          git diff --quiet --cached || git commit -m "Update news.json ($(date -u +'%Y-%m-%d %H:%M UTC'))"
          git push
